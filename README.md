# 🔍 EnvPeek

A lightweight Python CLI tool that checks whether environment variables are configured without displaying their actual values.

## Features

- Detect `.env` variables
- Identify empty values
- Keep secret values hidden
- No external dependencies

## Run

```bash
python main.py
